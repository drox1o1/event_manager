# Local dev / test environment — superset of every Lambda's runtime deps
# plus tooling. Mirrors src/layers/common/requirements.txt and each
# function's own requirements.txt so `pip install -r requirements/dev.txt`
# is enough to run tests and lint locally.

sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
pyjwt>=2.8,<3.0
bcrypt>=4.1,<5.0
pydantic[email]>=2.6,<3.0
aws-lambda-powertools>=2.38,<3.0
alembic>=1.13,<2.0
boto3>=1.34

pytest>=8.0
pytest-mock>=3.14
ruff>=0.4
moto[all]>=5.0
