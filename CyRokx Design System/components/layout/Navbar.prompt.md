Public site top navigation — wordmark, category links, city selector, search trigger. Sticky on scroll.

```jsx
<Navbar categories={['Music','Comedy','Workshops']} city="Mumbai" onSearchClick={openSearch} />
```
