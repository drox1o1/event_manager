A collapsible filter group for the listing sidebar — one per facet (category, city, date range, price).

```jsx
<FilterControl title="Category" icon="filter">
  <Checkbox label="Music" />
  <Checkbox label="Comedy" />
</FilterControl>
```
