The status pill used everywhere an event, order or organiser has a lifecycle state.

```jsx
<Badge status="live" />
<Badge status="review">In review</Badge>
```

Statuses map to the semantic tokens: draft/soldout → muted, review → warning, approved/live → success, rejected → error.
