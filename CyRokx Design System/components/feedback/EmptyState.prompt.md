Used for empty search results, empty attendee tables, empty moderation queue — icon, one line of message, optional action button.

```jsx
<EmptyState icon="calendar" title="No events yet" description="Create your first event to see it here." action={<Button>Create event</Button>} />
```
